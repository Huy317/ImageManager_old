public class explain {
    /*
    Tag is now an Object
    Tag will only exist once inside TagManager

    TagManager must only exist once
    so use TagManager.getInstance() instead of new TagManager

    Get the tag from the TagManager first then use it elsewhere
    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
     */
}
